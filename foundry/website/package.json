{
  "name": "andrewcheong-website",
  "private": true,
  "type": "module",
  "description": "andrewcheong.com static site — hxh OS + apps source, bundle, tests",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --import ./tests/loader.mjs --test tests/",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "jsdom": "^27.0.0"
  }
}
