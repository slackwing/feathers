# Database connection (should match docker-compose.yml)
DB_HOST=timescaledb
DB_PORT=5432
DB_NAME=sxiva_stats
DB_USER=sxiva_user
DB_PASSWORD=your_postgres_password_here

# API authentication token
# Generate a random token: openssl rand -hex 32
API_TOKEN=changeme-set-a-real-token-here
