<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">

    <changeSet id="006-add-abi-save-columns" author="sxiva">
        <comment>Add abi (floating point) and save (dollar amount) columns to daily_summary table</comment>
        <sql>
            ALTER TABLE daily_summary
            ADD COLUMN abi NUMERIC(4,1),
            ADD COLUMN save INTEGER;
        </sql>
        <rollback>
            ALTER TABLE daily_summary
            DROP COLUMN abi,
            DROP COLUMN save;
        </rollback>
    </changeSet>

</databaseChangeLog>
